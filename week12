##第一題
```C
#include <stdio.h>
char line[2000];
int main()
{
   for(int t=0;gets(line);t++){//Input
      int ans[256]={};
   for(int i=0;line[i]!=0;i++){
      char c=line[i];
      ans[c]++;
   }//字串迴圈
   if(t>0)printf("\n");//前面的空白
   for(int i=0;i<256;i++){//Output
   if(ans[i]>0)printf("%d %d\n",i,ans[i]);
   }
 }
}
```
